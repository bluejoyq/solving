import sys

def check_num(c):
    if c == "+" or c == "*" or c =="-":
        return 0
    return 1
def solve():
    N = int(input())
    expression = input()
    num_of_exp = N // 2 + 1
    exps = []
    for ch in expression:
        
solve()